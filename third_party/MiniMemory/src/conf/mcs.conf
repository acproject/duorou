bind 0.0.0.0
port 6385
requirepass password123
# 追加持久化（AOF），确保异常退出也能恢复
appendonly yes
# 显式设置 AOF 文件路径，避免工作目录差异导致找不到文件
appendfilename ./data/appendonly.aof
appendfsync everysec
# 持久化：通过配置 save 来设定触发条件
save 900 1
save 300 10
save 60 10000

# 内存管理
maxmemory 2gb

maxmemory-policy allkeys-lru